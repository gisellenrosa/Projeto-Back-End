import {generateId} from '../../services/idGenerate';
import connection from '../../data/BaseDatabase';
import {generateToken} from '../../services/generateToken';
import {PhotoRequestDTO, PhotoResponseDTO, Photo} from '../../model/photo';

export default async function createPhotoBusiness(userRequestData: PhotoRequestDTO): Promise<photoResponseDTO>{
 
}